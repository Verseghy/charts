apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: matverseny-backend

resources:
  - ../../../base/matverseny-admin

patches:
  - target:
      kind: Ingress
      name: matverseny-admin
    path: ingress-hostname-patch.yaml

