apiVersion: v1
kind: Service
metadata:
  name: matverseny-admin
  labels:
    app.kubernetes.io/name: matverseny-admin
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: matverseny-admin

