apiVersion: v1
kind: Service
metadata:
  name: {{ include "matverseny.fullname" . }}
  labels:
    {{- include "matverseny.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "matverseny.selectorLabels" . | nindent 4 }}
